  
<div ng-hide="Event.showEventDetail">
    <!-- Filters -->
    <filters-events on-filter="Event.searchEvent(Event.getMyEvents)"
                    on-clear-filter="Event.clearFilters()"
                    country-for-search="Event.filter.CountriesForSearch"
                    category-for-search="Event.filter.CategoriesForSearch"
                    name-for-search="Event.filter.IdentificadorParaBuscar"></filters-events>

    <div class="list-group">
        <div class="panel-body" style="padding:0px;">
            <ul class="list-group">
                <li class="list-group-item" ng-repeat="event in Event.myEventsList">
                    <img ng-src="{{event.Grupos.Personas.UrlImagenPerfil == '' ? '../Content/assets/img/demo/avatar.png' : event.Grupos.Personas.UrlImagenPerfil}}" style="float:left;width:100px; height:100px; border-radius:50%;" />
                    <div style="float:left; margin-left:10px;max-width: 80%;overflow: hidden;text-overflow: ellipsis;">
                        <a ng-click="Event.getEventDetail(event)" href="javascript:void(0)"><h2 class="color-info" ng-bind="event.Titulo"></h2></a>
                        <em class="lead" ng-bind="event.Descripcion" style="white-space: nowrap;"></em>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</div>
 <!-- Event detail -->
<div ng-show="Event.showEventDetail">
    <event-detail></event-detail>
</div>